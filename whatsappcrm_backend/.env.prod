# Backend Environment Variables
# This file now only contains non-secret environment variables and the Infisical token.
# All other secrets (DB_PASSWORD, DJANGO_SECRET_KEY, etc.) should be moved to your Infisical project.

# --- Infisical Service Token ---
# This token grants your application access to the secrets stored in Infisical.
# Replace the placeholder with your actual Universal Service Token from Infisical.
INFISICAL_TOKEN=your-infisical-service-token-goes-here

# --- Infisical Project Configuration ---
# These identify which project and environment to pull secrets from.
INFISICAL_PROJECT_ID=your-project-id-from-infisical
INFISICAL_ENVIRONMENT=prod

# --- Non-Secret Django Settings ---
DJANGO_DEBUG=False # This MUST be False in production for security.
DJANGO_ALLOWED_HOSTS='backend.hanna.co.zw,dashboard.hanna.co.zw,72.60.91.41,127.0.0.1,localhost'
CSRF_TRUSTED_ORIGINS='http://localhost:5173,http://127.0.0.1:5173,https://dashboard.hanna.co.zw,http://dashboard.hanna.co.zw,https://backend.hanna.co.zw,http://backend.hanna.co.zw'
CORS_ALLOWED_ORIGINS='https://dashboard.hanna.co.zw,http://dashboard.hanna.co.zw'
CORS_ALLOW_CREDENTIALS=True

# --- Docker-specific settings (can be overridden by Infisical if set there) ---
DB_HOST=db
DB_PORT=5432
